# Acceptance：移动端 AI 知识产品重构

状态：已批准；前端纵切已通过 Gate B，实现 revision `2662f58405c905e4b923870df46deca6a2128252`。未勾选项留待真实内容发布与迁移阶段。

## 视觉审核

- [x] 首页、搜索、板块首页、长文、页面树、AI 抽屉均有 360、390、430px 截图。
- [x] 每个页面保持单一主操作，文字层级与留白清晰。
- [x] 没有未批准的颜色、字体、图标、圆角、间距或阴影。
- [ ] 长文的多栏、表格、图片、嵌入和附件在手机可用。
- [ ] 真实 Notion 分割线和嵌套 callout 在 360/390/430px 保留原顺序且无文本损失。
- [ ] `写在前面` 的引用文字与两个嵌套 PDF 在 360/390/430px 保持原父子关系、原顺序和可点击入口；parity 记录中父 block ID 为 `2467d60a-0dda-809e-99c9-d5dfc89311bd`，两个附件 sibling index 分别为 0、1。
- [x] 每张基线截图均关联 `approval.md` 中人工批准的路径或链接；agent 不得自行接受截图差异。

## 交互审核

- [x] 页面树可打开、关闭、返回上级并保持焦点。
- [x] 关键词搜索只返回命中文档和段落，能跳转锚点。
- [x] 文档页 AI 默认包含当前页面和章节上下文。
- [x] 从来源进入文章后能返回原 AI 回答。
- [x] 使用固定 fixture 和网络 mock 验证：关键词搜索不调用 AI；含多个事实结论的 `grounded` 回答中每个 claim 都有锚点 citation；`insufficient` 回答没有事实性结论。

## 内容与可信度审核

- [x] 当前公开 fixture 页面不依赖读者的 Notion 登录权限；真实 Notion 发布链路留待 T1.2–T1.3。
- [x] 结构覆盖 fixture 中的文档块语义、内部链接和更新时间被保留。
- [x] 无来源或低置信 AI 回答不伪装为确定事实。

## 工程审核

- [x] Typecheck、单测、生产构建通过。
- [x] 关键页面有视觉回归检查；视觉基线由人工批准。
- [x] 本次改动可追溯到 requirements、design 与 tasks。
